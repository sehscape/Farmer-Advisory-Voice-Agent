[pytest]
# Unit tests live in tests/. The scripts/test_*.py files are standalone smoke
# tests (run them with python), so pytest must not import them.
testpaths = tests
markers =
    slow: requires model download or GPU – run with pytest -m slow
